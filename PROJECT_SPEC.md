# PoRoom — 온라인 뽀모도로 공동 작업실

## 한 줄 소개
화상회의 없이, 접속 상태/타이핑 여부/작업 시간/글자수만 실시간으로 공유하며
함께 뽀모도로 작업을 하는 웹 플랫폼.

## 기술 스택 (결정됨)
- 프론트엔드: Next.js (App Router) + Tailwind CSS
- 인증: Supabase Auth (Google OAuth)
- 데이터베이스: Supabase (PostgreSQL)
- 실시간 통신: Supabase Realtime (온라인 상태, 타이핑 여부, 채팅)
- 배포: Vercel (프론트) + Supabase (백엔드)

## 핵심 기능

### 1. 로그인
- 구글 계정으로 로그인. 로그인 후 첫 화면은 "포룸" 페이지.

### 2. 메뉴 구조
- **포룸**: 로그인 후 첫 페이지. 상단에 내가 입장해 있는 방 리스트 + 초대코드 입력 버튼(다른 방 입장용). 하단에 전체/추천 방 리스트.
- **랭킹**: 방 기준 전체 랭킹 / 개인 기준 전체 랭킹 두 개의 탭.
- **개인**: 계정 정보, 목표 현황(글자수/시간), 입장한 방 목록, 랭킹 현황(방 기준/전체 기준).

### 3. 방(Room) 시스템
- 방장이 방을 생성하면 고유 초대코드가 발급됨.
- 다른 사용자는 초대코드를 입력해야 입장 가능.
- 방 안 레이아웃:
  - 왼쪽: 채팅창
  - 가운데: 참여자 카드 목록 — 각 카드에 설정한 뽀모도로 시간, 경과 시간, 총 누적 작업시간, 작업 글자수 표시
  - 오른쪽: 내 뽀모도로 게이지바, 타이머 설정, 글자수 입력창, 방 내 랭킹, 월별/연별 글자수 통계

### 4. 실시간 상태 표시
표시할 상태값: 접속중 / 미접속 / 타이핑 중 / 휴식 중(타이핑 없음)
- 캠 없음. 오직 상태값 + 숫자만 실시간 브로드캐스트.
- 타이핑 감지는 브라우저 입력 이벤트 기반 활동 감지로 처리 (외부 앱 입력은 감지 불가 — 글자수는 사용자가 직접 입력하는 자기 신고 방식).

### 5. 랭킹 & 목표
- 전체 사용자 글자수/작업시간 랭킹
- 방별 글자수/작업시간 랭킹
- 개인 목표 설정 (글자수, 시간)
- 방 목표 설정 (글자수, 시간) — 방장이 설정
- 개인 간 대결, 방 간 대결 (글자수 또는 작업시간 기준)

## 데이터베이스 테이블 (초안)
- `users`: id, email, name, google_id, created_at
- `rooms`: id, name, invite_code, owner_id, goal_chars, goal_minutes, created_at
- `room_members`: room_id, user_id, joined_at
- `sessions`: id, user_id, room_id, start_time, end_time, chars_written, duration_minutes
- `goals`: user_id, target_chars, target_minutes, period(월/년)
- `chat_messages`: id, room_id, user_id, content, created_at

## 디자인 원칙
- 미니멀, 세련된 스타일. 불필요한 장식 없이 정보 위주.
- 컬러는 절제해서 사용 (강조색 1~2개 정도).

## 개발 순서 (단계별로 진행할 것)
1. Next.js 프로젝트 초기 세팅 + Supabase 연동 + Google 로그인
2. 포룸/랭킹/개인 페이지 라우팅 및 기본 레이아웃 (더미 데이터로)
3. 방 생성 + 초대코드 입장 로직 (DB 연동)
4. 방 안 화면: 참여자 카드, 채팅, 타이머 UI
5. Supabase Realtime으로 온라인/타이핑 상태, 채팅 실시간 연동
6. 글자수/시간 기록 저장 + 랭킹 집계 쿼리
7. 목표 설정 + 대결 기능
8. UI 다듬기 (미니멀 디자인 적용), 반응형 대응

각 단계가 끝나면 로컬에서 실행 확인 후 다음 단계로 진행할 것. 한 번에 여러 단계를 묶어서 진행하지 말 것.

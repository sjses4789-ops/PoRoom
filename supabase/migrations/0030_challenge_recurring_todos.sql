-- PoRoom: 챌린지 방(5천자/1만자/초단 완고)에 참여하면 "매일 5천자 쓰기"
-- 같은 항목이 할 일 목록에 자동으로 추가된다. 하루(또는 초단 완고는
-- 그 달) 단위로 다시 나타나야 하므로, 자동 추가된 항목이 "며칠치"인지
-- 구분할 수 있는 날짜 컬럼을 둔다 — 체크(삭제)해도 다음 날 새 행이
-- 새로 추가되도록.
alter table public.todos
  add column if not exists for_date date;
